# Ambiente de Produção - Render
# ⚠️ NUNCA COMMITAR ESTE ARQUIVO COM VALORES REAIS!
# Este é apenas um template. Adicione ao .gitignore.

# Ambiente
NODE_ENV=production

# Porta (Render controla automaticamente, mas pode especificar)
# PORT=3001

# Banco de Dados - MongoDB Atlas (Produção)
# Formato: mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/DATABASE?retryWrites=true&w=majority
# ⚠️ Se senha tiver caracteres especiais (@:/?#%), fazer URL encode!
# Exemplo: Pass@123 vira Pass%40123
# ⚠️ Use um cluster dedicado para produção (não o free tier se possível)
DATABASE_URL="mongodb+srv://USER:PASSWORD@CLUSTER.mongodb.net/eletrostart?retryWrites=true&w=majority"

# JWT - GERAR NOVO SECRET PARA PRODUÇÃO!
# Comando: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET="[GERAR-SECRET-SEGURO-64-CARACTERES-MINIMO]"
JWT_EXPIRES_IN="7d"

# Discord Bot
DISCORD_BOT_TOKEN="[SEU-TOKEN-DO-DISCORD]"
DISCORD_CLIENT_ID="[SEU-CLIENT-ID]"
DISCORD_CHANNEL_ID="[ID-DO-CANAL]"

# Admin padrão - TROCAR SENHA APÓS PRIMEIRO LOGIN!
ADMIN_EMAIL="admin@eletrostart.com.br"
ADMIN_PASSWORD="[TROCAR-SENHA-FORTE]"

# Frontend URL (Netlify)
FRONTEND_URL="https://seu-site.netlify.app"

# ============================================
# INSTRUÇÕES DE USO:
# ============================================
# 1. NÃO use este arquivo localmente (use .env com SQLite)
# 2. Configure estas variáveis no Render Dashboard → Environment
# 3. Valores entre colchetes [] devem ser substituídos
# 4. Após configurar no Render, DELETE este arquivo ou mantenha vazio
# ============================================
